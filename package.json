{
  "name": "redux-effects-aws",
  "description": "AWS SDK effect driver.",
  "repository": "git://github.com/redux-effects/redux-effects-aws.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-object": "^1.1.1",
    "@f/thunk-to-promise": "^1.1.1",
    "@yaws/read-region": "^1.0.1",
    "aws-sdk": "^2.2.28"
  },
  "devDependencies": {
    "@f/bind-middleware": "^1.1.1",
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "tape": "^4.2.0"
  }
}
